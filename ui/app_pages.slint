// 与 src/state/mod.rs 的 PAGE_* 保持一致。2 曾为设置页，已删除，勿复用。
export global AppPages {
    out property <int> dashboard: 0;
    out property <int> modbus: 1;
    out property <int> firmware: 3;
    out property <int> device-config: 4;
    out property <int> external: 5;
}
