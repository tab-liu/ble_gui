版本：904101(第一版本)
功能说明：1、支持ble4.2，mtu协定为247，申请连接间隔为15ms；
2、透传功能(下行)，将Bluetti APP写0xFF02服务的数据接收完，再统一通过串口(波特率115200)发出；
3、透传功能(上行)，接收串口数据，再统一通过0xff01服务notify给手机Bluetti app。
4、预留OTA蓝牙模块估计功能，服务为0xff04，通过xmodem协议传输文件，支持modbus通信。

版本：904601(第二版本)——2023/03/10
功能说明：1、支持ble4.2，mtu协定为247，申请连接间隔为15ms；
2、透传功能(下行)，将Bluetti APP写0xFF02服务的数据接收完，再统一通过串口(波特率115200)发出；
3、透传功能(上行)，接收串口数据，再统一通过0xff01服务notify给手机Bluetti app。
4、增加OTA功能，基于原0xff02服务截取APP的升级命令，在原通道与APP通过xmodem协议传输文件，增加固件验签功能，支持modbus通信。
5、增加握手鉴权ECC、通信加密AES_CBC、安全启动功能ECC。

版本：904602(第三版本)——2023/08/01
功能说明：1、支持ble4.2，mtu协定为247，申请连接间隔为15ms；
2、透传功能(下行)，将Bluetti APP写0xFF02服务的数据接收完，再统一通过串口(波特率115200)发出；
3、透传功能(上行)，接收串口数据，再统一通过0xff01服务notify给手机Bluetti app。
4、增加OTA功能，基于原0xff02服务截取APP的升级命令，在原通道与APP通过xmodem协议传输文件，增加固件验签功能，支持modbus通信。
5、增加握手鉴权ECC、通信加密AES_CBC、安全启动功能ECC。
6、增加蓝牙直连密码防护功能，APP用户密码登录后，直连设备可进入设备界面设置设备连接密码（只有APP密码登录者有此权限），若已设置设备连接密码，游客模式连接此设备需输入密码。



最终确认版本：904602(第三版本)——2023/08/01
备注：因未加密版本为9044.02，加密版首次提交测试部版本应为9046.01，所以第三版本9046.02更改为9046.01，功能是第三版本的功能；

版本：904602(正式版本)——2024/11/27
1、增加modbus-slave address 动态适配（DC HUB）
2、修复升级卡包问题，串口接收中断里 不调 软件定时器API，改由SEVICE 定时器周期判断实现；

版本：904602(正式版本)——2025/01/09
3、在11/27版本中增加21000段FF03通道主动上报功能，self_BleSendBytes()，底层主动写多字节均视为21000段主动上报;

版本：904603(正式版本)——2025/01/12
4、在01/09版本中增加FLASH补丁，基于夏杰发的Flash_Update_Patch-20240401替换FLASH相关文件，已验证升级相关无问题；
5、在01/09版本中修改LVD电压检测SYSCFG_LVD_CTL，提高为5档，在main函数中上电调用；

版本：904603(正式版本)——2025/02/17
6、上电增加协议版本询问指令重发，直到回复结束询问；

